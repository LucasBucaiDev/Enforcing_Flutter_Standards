---
name: flutter-layout-diagnostics
description: Use for concrete Flutter layout failures involving RenderFlex overflow, unbounded constraints, viewport sizing, text scaling, or constraint negotiation. Route through the shared evidence workflow and Flutter standards while loading only the focused layout-diagnostics recipe.
---

# Flutter Layout Diagnostics

## Compose

Require `evidence-driven-development` and `enforcing-flutter-standards`.
If either required skill is unavailable, report the missing skill and stop.
Tell the user to make it available before retrying.
Do not recreate either skill's workflow or domain policy locally.

## Route

Let the process skill select the current phase contract. Let the Flutter skill
own repository discovery and domain decisions. Select only
`layout-diagnostics.md` for an observable layout failure.

Keep this entrypoint thin. Do not restate approval, diagnosis, verification,
or shared Flutter rules. Do not load adjacent recipes without their own
observable predicate.
